import React from "react";
import "./styles.scss";
export declare const Row: ({ cols, small, medium, large, children, }: {
    cols: number;
    children: React.ReactNode[];
    small?: number;
    medium?: number;
    large?: number;
}) => JSX.Element;
