/// <reference types="react" />
import "./styles.css";
import { ITable } from "./types";
export declare const Table: ({ data, loading, allowOpen, allowAdd, allowEdit, allowDelete, fields, applyFilter, searchFilters, renderCustomRowButtons, onChangePage, }: ITable) => JSX.Element;
