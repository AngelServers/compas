/// <reference types="react" />
import "./styles.css";
import { ITable } from "./types";
export declare const Table: ({ data, loading, allowOpen, addButton, allowAdd, allowEdit, allowDelete, fields, style, applyFilter, searchFilters, renderCustomRowButtons, onChangePage, skeletonRows, outline, }: ITable) => JSX.Element;
